import 'package:flutter/material.dart';
import 'package:videostitch/widgets/MainView/main_view.dart';

class MainScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: MainView(),
    );
  }
}
